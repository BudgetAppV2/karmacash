import { describe, it, expect } from 'vitest';

describe('Basic Firestore Security Rules', () => {
  // Basic test to make sure Vitest is working
  it('should pass a simple test', () => {
    expect(true).toBe(true);
  });
}); 